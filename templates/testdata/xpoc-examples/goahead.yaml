http:
    - matchers:
        - dsl:
            - contains(to_lower(server), "goahead")
          type: dsl
      method: GET
      path:
        - '{{BaseURL}}/'
id: embedthis-goahead
info:
    author: xray-converter
    metadata:
        cpe: embedthis:goahead
    name: Embedthis GoAhead
    severity: info
    tags: neutron,xray,converted
